[build]
builder = "nixpacks"

[build.nixpacks]
aptPkgs = ["chromium", "fonts-noto-cjk"]

[deploy]
startCommand = "export CHROMIUM_PATH=$(find /nix -name 'chromium' -type f 2>/dev/null | head -1) && npx prisma db push && npm start"
